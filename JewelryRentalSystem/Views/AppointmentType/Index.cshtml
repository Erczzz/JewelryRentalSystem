@model IEnumerable<JewelryRentalSystem.Models.AppointmentType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.APTName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.APTDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.APTName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.APTDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AppointmentTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AppointmentTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AppointmentTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
